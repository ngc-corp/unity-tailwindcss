plugin(function ({ addUtilities }) {
  const transitionUtilities = {
  };

  // Add the utilities (responsive, hover, focus, etc.)
  addUtilities(transitionUtilities, ['responsive', 'hover', 'focus', 'active']);
})
